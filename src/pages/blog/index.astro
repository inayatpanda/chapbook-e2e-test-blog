---
import { withBase } from '../../lib/withBase';
// The blog now lives at the site root. Send /blog/ → / (base-aware for a
// GitHub Pages sub-path; no-op at base '/').
return Astro.redirect(withBase('/'), 301);
---
